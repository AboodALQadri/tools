import 'package:flutter/material.dart';

class BorrowedTab extends StatelessWidget {
  const BorrowedTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('AcceptedTab'),
    );
  }
}
