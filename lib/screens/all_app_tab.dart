import 'package:flutter/material.dart';

class AllAppTab extends StatelessWidget {
  const AllAppTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('grgrfgrfgrgr'),
    );
  }
}
